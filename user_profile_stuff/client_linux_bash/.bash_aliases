#Aliases


alias ls="ls -alh --color=tty"

alias ifconfig="ip address show"

alias docker="sudo docker"

alias aup="sudo docker compose up -d --remove-orphans"
alias adown="sudo docker compose down --remove-orphans"
alias als="sudo docker container ls -a"
alias abuild="sudo docker compose build"


